import styled from '@emotion/styled';
import {
  space,
  typography,
  layout,
  flexbox,
  grid,
  border,
} from 'styled-system';

const Box = styled('div')(space, typography, layout, flexbox, grid, border);

export default Box;
